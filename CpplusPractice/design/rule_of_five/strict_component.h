/**
 * 
 */
template<class TYPE>
class lock_pair {
private:

// internal method :

public:
// basic method :

// custom method :

// any copy and move operation is not recommended :
    lock_pair(const lock_pair&)         = delete;
    lock_pair(lock_pair&&)              = delete;
    void operator=(const lock_pair&)    = delete;
    void operator=(lock_pair&&)         = delete;
};
